package main

const (
	zipFileName          = `Animal.zip`
	listTXTFileName      = `list.txt`
	baseDirectoryFormat  = `%s/modul2`
	writeTXTStringFormat = `UID_%s_%s`

	land  = `darat`
	water = `air`
	bird  = `bird`

	regexFileName = `([^\\]+$)`
	newLine       = "\n"
)
